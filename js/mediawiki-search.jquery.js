var mediaWikiSearch = function() {


	// Makes a search request and jqXHR object
	// Params:
	// 	host: Hostname for the mediawiki you're making an api call to, e.g. wikipedia.com
	//  term: Search term
	//  limit: Number of results
	var makeSearchRequest = function(host, term, limit) {


	}

	return {
		search: makeSearchRequest
	}

}();